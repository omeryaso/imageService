@model ImageServiceWeb.Models.Config




@{
    ViewBag.Title = "Config";
}

<h2>Create</h2>

@using (Html.BeginForm()) 
{
    @Html.AntiForgeryToken()
    
    <div class="form-horizontal">
        <h4>Config</h4>
        <hr />

        @Html.ValidationSummary(true, "", new { @class = "text-danger" })
        <div class="form-group">
            @Html.LabelFor(m => m.OutputDirectory, new { @class = "col-md-2 control-label" })
            <div class="col-md-10">
                @Html.DisplayFor(m=> m.OutputDirectory, new { @class = "col-md-2 control-label" })
            </div>
            @Html.LabelFor(m => m.LogName, new { @class = "col-md-2 control-label" })
            <div class="col-md-10">
                @Html.TextBoxFor(m => m.LogName, new { @class = "form-control" })
            </div>
            @Html.LabelFor(m => m.ThumbnailSize, new { @class = "col-md-2 control-label" })
            <div class="col-md-10">
                @Html.TextBoxFor(m => m.ThumbnailSize, new { @class = "form-control" })
            </div>
           
        </div>

        <table class="table">
            <tr>
                <th>
                    @Html.DisplayNameFor(model => model.Handlers)
                </th>
            </tr>

            @foreach (var item in Model.Handlers)
            {
                <tr>
                    <td>
                        @Html.DisplayFor(modelItem => item)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item)
                    </td>
                    <td>
                        @*@Html.ActionLink("Edit", "Edit", new { id = item.ID }) |
                        @Html.ActionLink("Logs", "Logs", new { /* id=item.PrimaryKey */ }) |
                        @Html.ActionLink("Delete", "Delete", new { id = item.ID })*@
                    </td>
                </tr>
            }

        </table>

        
        @*<div class="form-group">
            <div class="col-md-offset-2 col-md-10">
                <input type="submit" value="Config" class="btn btn-default" />
            </div>
        </div>*@
    </div>
}

<div>
    @Html.ActionLink("Back to List", "ImageWeb")
</div>
